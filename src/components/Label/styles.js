import styled from "styled-components";

export const LabelComponent = styled.label`
    position: absolute;
    color: rgba(0, 0, 0, 0.5);
    top: 0;
    left: 0;
    font-size: 16px;
    padding: 2px 20px;
`